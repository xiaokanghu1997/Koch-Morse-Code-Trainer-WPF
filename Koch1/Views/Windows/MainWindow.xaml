<ui:FluentWindow
    x:Class="Koch.Views.Windows.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Koch.Views.Windows"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="{Binding ViewModel.ApplicationTitle, Mode=OneWay}"
    MinWidth="800"
    MinHeight="300"
    MaxWidth="800"
    MaxHeight="300"
    d:DataContext="{d:DesignInstance local:MainWindow, IsDesignTimeCreatable=True}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    ui:Design.Background="{ui:ThemeResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"
    ExtendsContentIntoTitleBar="False"
    Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"
    WindowBackdropType="None"
    WindowCornerPreference="Round"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">

    <Grid>
        <!--定义网格-->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <!--设置标题栏-->
        <ui:TitleBar
            x:Name="TitleBar"
            Grid.Row="0"
            ShowMaximize="False"
            CloseWindowByDoubleClickOnIcon="True"
            Padding="0"
            Height="30">
            <ui:TitleBar.Header>
                <Grid Height="30" VerticalAlignment="Stretch">
                    <StackPanel 
                        Orientation="Horizontal" 
                        VerticalAlignment="Center">
                        <Viewbox Width="18" Height="18" Margin="6,6,6,6">
                            <Path
                                Data="{DynamicResource Logo}"
                                Fill="{ui:ThemeResource TextFillColorPrimaryBrush}"
                                Stretch="Uniform"/>
                        </Viewbox>
                        <TextBlock 
                            Text="{Binding ViewModel.ApplicationTitle}"
                            VerticalAlignment="Center"
                            FontSize="11"
                            FontWeight="Normal" />
                    </StackPanel>
                </Grid>
            </ui:TitleBar.Header>
        </ui:TitleBar>
    </Grid>
</ui:FluentWindow>